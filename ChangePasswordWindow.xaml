<Window x:Class="TextNormalizer.ChangePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TextNormalizer"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Смена пароля" Height="300" Width="450" ResizeMode="NoResize">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <PasswordBox x:Name="OldPasswordBox" Margin="0 5" materialDesign:HintAssist.Hint="Старый пароль"></PasswordBox>
            <PasswordBox x:Name="NewPasswordTextbox" Margin="0 5" materialDesign:HintAssist.Hint="Новый пароль"></PasswordBox>
            <PasswordBox x:Name="NewPasswordConfirmTextbox" Margin="0 5" materialDesign:HintAssist.Hint="Подтверждение нового пароля"></PasswordBox>
            <Button x:Name="ConfirmButton" Margin="0 5" Content="Подтвердить" Click="ConfirmButton_Click"></Button>
        </StackPanel>
    </Grid>
</Window>
